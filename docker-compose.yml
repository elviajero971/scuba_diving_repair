version: '3.8'

services:
  # Rails app service
  app:
    build: .
    environment:
      - RAILS_ENV=production
      - DATABASE_HOST=db
    env_file:
      - /app/.env  # Reference the new .env file inside the container
    depends_on:
      - db
    volumes:
      - ".:/app"  # Mount the app directory

  # PostgreSQL service
  db:
    image: postgres:13
    environment:
      POSTGRES_DB: ${SCUBADIVING_APP_DATABASE_NAME}
      POSTGRES_USER: ${SCUBADIVING_APP_DATABASE_USER}
      POSTGRES_PASSWORD: ${SCUBADIVING_APP_DATABASE_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
