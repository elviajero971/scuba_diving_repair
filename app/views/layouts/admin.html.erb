<!DOCTYPE html>
<html lang="en">
<head>
  <title>ScubaDivingShop</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body class="h-screen flex overflow-hidden bg-gray-100">
<!-- Sidebar -->
<div class="w-64 bg-gray-800 text-gray-100">
  <div class="p-4 text-center text-xl font-bold">Admin Panel</div>
  <nav class="flex flex-col space-y-1">
    <%= link_to 'Dashboard', admin_root_path, class: 'block py-2.5 px-4 hover:bg-gray-700' %>
    <%= link_to 'Gears', admin_gears_path, class: 'block py-2.5 px-4 hover:bg-gray-700' %>
    <%= link_to 'Brands', admin_brands_path, class: 'block py-2.5 px-4 hover:bg-gray-700' %>
    <%= link_to 'Services', admin_services_path, class: 'block py-2.5 px-4 hover:bg-gray-700' %>
  </nav>
</div>

<!-- Main Content -->
<div class="flex-1 overflow-auto">
  <header class="bg-white shadow-sm">
    <div class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
      <h1 class="text-3xl font-semibold text-gray-900">
        Admin Dashboard
      </h1>
    </div>
  </header>
  <main class="py-8 px-4 sm:px-6 lg:px-8">
    <%= render "shared/flash_message" %>
    <%= yield %>
  </main>
</div>
</body>
</html>
