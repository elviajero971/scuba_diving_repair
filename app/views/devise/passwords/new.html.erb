<div class="min-h-screen flex flex-col justify-center items-center">
  <h2 class="text-2xl font-bold text-center mb-6">Forgot your password?</h2>

  <div class="w-full max-w-md p-8">
    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="mb-4">
        <%= f.label :email, class: 'block text-gray-700 font-medium' %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "w-full px-4 py-2 mt-1 border rounded-lg" %>
      </div>

      <div class="text-center">
        <%= f.submit "Send me reset password instructions", class: "bg-blue-500 text-white px-4 py-2 rounded-lg hover:bg-blue-600" %>
      </div>
    <% end %>

    <div class="text-center mt-6">
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
