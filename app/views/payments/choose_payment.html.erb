<div class="max-w-2xl mx-auto mt-10 bg-white shadow-lg rounded-lg p-6">
  <h2 class="text-2xl font-bold text-center mb-6">Choose Your Service Option</h2>

  <div class="mb-4">
    <h3 class="text-xl font-semibold">Service Details</h3>
    <p><strong>Gear:</strong> <%= @service.gear.name %></p>
    <p><strong>Created At:</strong> <%= @service.created_at.strftime("%B %d, %Y") %></p>
  </div>

  <div class="mt-6 text-center">
    <p class="mb-4">Choose your service type:</p>

    <%= button_to 'Pay for Basic Repair (1 month)', service_payments_path(@service, product_id: @product_basic.id), method: :post, class: 'bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded', data: { turbo: false } %>
    <%= button_to 'Pay for Premium Repair (2 weeks)', service_payments_path(@service, product_id: @product_premium.id), method: :post, class: 'bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded', data: { turbo: false } %>
  </div>
</div>
